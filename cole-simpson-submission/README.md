# Cole Simpson Code Challenge Submission

The main file, `serp.js`, handles parsing HTML content, while `serp.test.js` contains Jest tests to verify its functionality. The main file will return JSON.

## Installation

Before running the tests, install all required dependencies from `package.json`:

```sh
npm install
```

## Testing

```sh
NODE_OPTIONS=--experimental-vm-modules npx jest
```