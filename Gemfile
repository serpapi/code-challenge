# frozen_string_literal: true

source 'https://rubygems.org'

ruby '3.2.2'
gem 'zeitwerk', '~> 2.6'

# Parsing
gem 'nokogiri', '~> 1.15'

# Performance
# gem 'oj' # Not needed, but faster than the default JSON parser

group :development do

  # Linting
  gem 'rubocop', '~> 1.56'
  gem 'reek', '~> 6.1'
end

group :test do
  gem 'rspec', '~> 3.12'
end
